---
title: Phase 1.5 技术债清理实施计划
description: 文件操作系统完成后的收尾归档、遗留小修、测试补全、扫描性能优化与 imageStore 拆分
type: plan
status: current
updated: 2026-08-30
---

# Phase 1.5 技术债清理实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不引入新功能的前提下，清理文件操作系统（Phase 1）落地后的全部技术债：收尾归档、修复 4 个 deferred minor、补全测试与性能验证、优化扫描性能、拆分 619 行巨石 `imageStore`。

**Spec:** `docs/roadmap.md` 第七节（工程化任务）+ 第九节（架构隐患：imageStore 巨石）+ `.superpowers/sdd/phase1-file-operations/progress.md` 中的 deferred 项

**背景核实结论（2026-08-30，已对照代码验证）：**
- Phase 1 全部 12 Task 完成，60/60 测试通过，安装包已生成
- ledger 中标记的 2 个"缺口"（Shift 范围选择、MasonryGrid 多选）**实际已在后续 fix round 实现**：`selectRange`/`lastSelectedPath` 已落地于 `imageStore.ts` L131/L133/L701-711，`ImageGrid.tsx` L116-134 与 `MasonryGrid.tsx` L150-167 均已接入。本计划只需回归验证，无需重新实现
- `scanner.ts` 已有 `Promise.allSettled` 并行（L135）与「文件大小+修改时间」双条件跳过；hash 窗口仍为 1MB（L400）
- `imageStore.ts` 619 行，`ImageState` 接口承载 60+ 字段/方法，确认需要拆分

## Global Constraints

- 性能红线：启动 <3s、内存 <500MB、滚动帧率 ≥30 FPS，任何优化不得突破
- 所有命令前需 `conda activate imageviewer`；Windows PowerShell 语法（`;` 分隔，`$env:` 变量）
- 不新增第三方依赖
- 每个 Task 结束跑一次 `npx vitest run` + `npm run build` 确认不回归

## File Structure

```
新增：
├── src/stores/selectionStore.ts               # 多选状态（从 imageStore 拆出）
├── src/stores/viewStore.ts                    # 视图/布局/侧边栏状态（从 imageStore 拆出）
├── src/stores/__tests__/selection.test.ts     # 多选逻辑单元测试
└── docs/archive/文件操作阶段测试执行记录.md     # 归档用例执行结果

修改：
├── src/main/services/database.ts              # getDeletedFiles 加 libraryId 过滤、事务注释
├── src/main/ipc/file-handlers.ts              # 合并双重库查询、透传 libraryId
├── electron/preload.ts                        # getDeletedFiles 签名更新
├── src/types/index.ts                         # ElectronAPI.getDeletedFiles 签名更新
├── src/components/file-ops/RecycleBinView.tsx # 按当前库过滤删除记录
├── src/main/services/scanner.ts               # hash 窗口 64KB、批处理确认
├── src/main/services/__tests__/file-service.test.ts # 补充边界用例
├── src/stores/imageStore.ts                   # 移除已拆出的状态与方法
├── src/components/ImageGrid.tsx               # 迁移到 selectionStore
├── src/components/MasonryGrid.tsx             # 迁移到 selectionStore
├── src/components/file-ops/BatchRenameDialog.tsx # 迁移选择状态来源（如有引用）
└── 其余消费 viewMode/sidebarOpen/gridLayoutMode 的组件（迁移到 viewStore）

文档：
├── docs/roadmap.md                            # 回写进度（#2/#3/#7/#8 ✅、工程化勾选、架构隐患消项）
└── docs/archive/superpowers/plans/2026-08-22-phase1-file-operations.md  # 计划归档
```

**执行顺序：** Task 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8（Task 1-5 低风险可快速推进；Task 6-7 为重构，放最后并逐个回归）

---

## Task 1: Phase 1 收尾与文档归档

**Files:**
- Modify: `docs/roadmap.md`
- Move: `docs/superpowers/plans/2026-08-22-phase1-file-operations.md` → `docs/archive/superpowers/plans/`

**设计要点：**
- 按 `docs/README.md` 治理规范第 4 条：已完成的计划连同结果记录移入 `archive/` 并标记 `status: archived`
- roadmap 是唯一规划来源，进度必须回写，否则下一轮规划会重复已完成任务

- [ ] **Step 1: 移动计划文档并标记归档**

```powershell
Move-Item docs\superpowers\plans\2026-08-22-phase1-file-operations.md docs\archive\superpowers\plans\
```

修改归档文档头部：若有 front matter 加 `status: archived`；若无 front matter，按规范补充（`type: plan`、`status: archived`、`updated: 2026-08-30`）。

- [ ] **Step 2: roadmap.md 进度回写**

修改点：
1. 第二节表格：#7、#8 进度列改 ✅
2. 第一节 #2（设置壁纸）、#3（图片文件操作）标题后加 ✅，正文首行加「**实施**：2026-08-30 完成，12 Task 全部落地，60/60 测试通过」；#3 的实施计划链接改为 `archive/superpowers/plans/2026-08-22-phase1-file-operations.md`
3. 第一节末尾「当前进度」引用块更新为：第二轮（文件操作）已完成，第三轮为技术债清理（指向本计划）
4. 第七节工程化任务：`scripts/` 目录条目改为已完成（`generate-test-data.cjs` 已创建）；测试用例条目待 Task 4 执行后再勾
5. 第九节架构隐患表：「收藏/历史以 image_path 字符串为主键」行标注已解决（路径级联已实现）；「无配置持久化层」行标注已解决（electron-store 已引入）；「imageStore 巨石 store」行更新为「拆分中（见本计划）」
6. 第十节推荐执行顺序：第二轮加删除线标记完成
7. front matter `updated` 改为当天日期

- [ ] **Step 3: Commit**

```powershell
git add docs/
git commit -m "docs: Phase 1 收尾 — 计划归档与 roadmap 进度回写"
```

---

## Task 2: 遗留 minor 修复（4 项）

**Files:**
- Modify: `src/main/services/database.ts`
- Modify: `src/main/ipc/file-handlers.ts`
- Modify: `electron/preload.ts`
- Modify: `src/types/index.ts`
- Modify: `src/components/file-ops/RecycleBinView.tsx`
- Test: `src/main/services/__tests__/database-cascade.test.ts`（补充断言）

来自 `.superpowers/sdd/phase1-file-operations/progress.md` 的 4 个 deferred 项。

### 2.1 getDeletedFiles 增加 libraryId 过滤

- [ ] **Step 1: database.ts** — `getDeletedFiles` 增加可选 `libraryId` 参数：

```typescript
getDeletedFiles(limit: number = 100, libraryId?: number): Array<{...}> {
  if (!this.db) return [];
  if (libraryId !== undefined) {
    return this.db.prepare(
      'SELECT * FROM deleted_files WHERE library_id = ? ORDER BY deleted_at DESC LIMIT ?'
    ).all(libraryId, limit) as any[];
  }
  return this.db.prepare('SELECT * FROM deleted_files ORDER BY deleted_at DESC LIMIT ?').all(limit) as any[];
}
```

- [ ] **Step 2: file-handlers.ts** — handler 透传：

```typescript
ipcMain.handle('getDeletedFiles', async (_e, libraryId?: number, limit?: number) => {
  return getMasterDB().getDeletedFiles(limit, libraryId);
});
```

- [ ] **Step 3: preload.ts + types/index.ts** — 签名同步为 `getDeletedFiles: (libraryId?: number, limit?: number) => Promise<DeletedFileRecord[]>`。

- [ ] **Step 4: RecycleBinView.tsx** — 从 `useImageStore` 读取 `currentLibraryId`，请求时传入（收藏库 `FAVORITE_LIBRARY_ID` 或 `null` 时传 `undefined` 显示全部）。

### 2.2 file-handlers 双重库查询合并

- [ ] **Step 5:** `selectDestinationFolder` handler 中 `validateLibraryAccess(libraryId)` 后又 `getMasterDB().getLibrary(libraryId)` 查询了一次。改造 `validateLibraryAccess` 返回 `Library | null`：

```typescript
function getValidLibrary(libraryId: number) {
  return getMasterDB().getLibrary(libraryId);
}
```

`selectDestinationFolder` 内一次查询复用；其余 handler 保持 `if (!getValidLibrary(libraryId)) return ...` 用法。

### 2.3 事务包裹审查与注释

- [ ] **Step 6:** 审查 `database.ts` 中 Phase 1 新增方法的事务使用。`updateImagePath`（favorites + history 两条 UPDATE）与 `updateFolderPath`（多表多语句）的事务包裹**是合理的，保留**，并在方法注释中说明「事务用于保证级联更新的原子性」；如存在单语句被事务包裹的情况则移除。

### 2.4 重复注释清理

- [ ] **Step 7:** 检查 `file-handlers.ts` 与 `file-service.ts` 中重复的方法说明注释（如方法 JSDoc 与行内注释重复描述同一逻辑），保留 JSDoc 一处。

### 验证与提交

- [ ] **Step 8: 测试** — `database-cascade.test.ts` 中 `deleted_files` 用例补充：插入两个不同 `library_id` 的记录，断言 `getDeletedFiles(100, 1)` 只返回库 1 的记录。

```powershell
conda activate imageviewer
npx vitest run src/main/services/__tests__/database-cascade.test.ts
npm run build
```

- [ ] **Step 9: Commit**

```powershell
git add src/ electron/
git commit -m "fix: getDeletedFiles 按库过滤、合并重复库查询、事务与注释清理"
```

---

## Task 3: 单元测试补充

**Files:**
- Create: `src/stores/__tests__/selection.test.ts`
- Modify: `src/main/services/__tests__/file-service.test.ts`

**设计要点：**
- 多选逻辑是多库批量操作的入口，边界（反向区间、无锚点、重复切换）需要测试固化，为 Task 6 拆分提供安全网
- vitest include 为 `src/**/*.{test,spec}.{ts,tsx}`，`src/stores/__tests__/` 下可被收集

- [ ] **Step 1: 多选逻辑测试**（当前实现仍在 imageStore，Task 6 拆分时迁移此文件）

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { useImageStore } from '../imageStore';

describe('多选逻辑', () => {
  beforeEach(() => {
    useImageStore.getState().clearSelection();
  });

  it('toggleSelection 切换选中并记录锚点', () => {
    const s = useImageStore.getState();
    s.toggleSelection('a.jpg');
    expect(useImageStore.getState().selectedPaths.has('a.jpg')).toBe(true);
    expect(useImageStore.getState().lastSelectedPath).toBe('a.jpg');
    useImageStore.getState().toggleSelection('a.jpg');
    expect(useImageStore.getState().selectedPaths.has('a.jpg')).toBe(false);
  });

  it('selectRange 正向区间', () => {
    const all = ['a.jpg', 'b.jpg', 'c.jpg', 'd.jpg'];
    useImageStore.getState().selectRange('a.jpg', 'c.jpg', all);
    expect(useImageStore.getState().getSelectedPaths().sort()).toEqual(['a.jpg', 'b.jpg', 'c.jpg']);
  });

  it('selectRange 反向区间', () => {
    const all = ['a.jpg', 'b.jpg', 'c.jpg'];
    useImageStore.getState().selectRange('c.jpg', 'a.jpg', all);
    expect(useImageStore.getState().selectedPaths.size).toBe(3);
  });

  it('selectRange 端点不存在时不改动选择', () => {
    useImageStore.getState().toggleSelection('a.jpg');
    useImageStore.getState().selectRange('a.jpg', 'missing.jpg', ['a.jpg', 'b.jpg']);
    expect(useImageStore.getState().selectedPaths.size).toBe(1);
  });

  it('selectRange 与已有选择叠加', () => {
    useImageStore.getState().toggleSelection('x.jpg');
    useImageStore.getState().selectRange('a.jpg', 'b.jpg', ['a.jpg', 'b.jpg', 'c.jpg']);
    expect(useImageStore.getState().selectedPaths.size).toBe(3);
  });
});
```

注意：`selectRange` 测试不依赖 `images`；`selectAll` 依赖 `images` 数组，测试前 `useImageStore.setState({ images: [...] as any })` 注入。

- [ ] **Step 2: file-service 边界用例补充**（追加到现有 `file-service.test.ts`）

```typescript
it('moveFiles 到库根目录（空目标路径）不产生 ./ 前缀', async () => {
  fs.writeFileSync(path.join(libDir, 'photos', 'mv.jpg'), 'x');
  const result = await fileService.moveFiles(1, ['photos/mv.jpg'], '');
  expect(result.succeeded).toHaveLength(1);
  const thumbsDB = getThumbnailsDB(libDir);
  expect(thumbsDB.getImages({ limit: 10, offset: 0 })[0].relative_path).toBe('mv.jpg');
});

it('copyFiles 目标已存在时不覆盖', async () => {
  fs.writeFileSync(path.join(libDir, 'src.jpg'), 'source');
  fs.mkdirSync(path.join(libDir, 'dest'));
  fs.writeFileSync(path.join(libDir, 'dest', 'src.jpg'), 'existing');
  const result = await fileService.copyFiles(1, ['src.jpg'], 'dest');
  expect(result.failed).toHaveLength(1);
  expect(fs.readFileSync(path.join(libDir, 'dest', 'src.jpg'), 'utf8')).toBe('existing');
});
```

- [ ] **Step 3: 运行全部测试**

```powershell
conda activate imageviewer
npx vitest run
```

- [ ] **Step 4: Commit**

```powershell
git add src/
git commit -m "test: 补充多选逻辑与文件操作边界用例"
```

---

## Task 4: 归档测试用例执行与记录

**Files:**
- Create: `docs/archive/文件操作阶段测试执行记录.md`

**设计要点：**
- 执行 `docs/archive/测试方案.md` 中 2026-08-16 遗留的 9 个用例：`TC-THUMB-004/005`、`TC-GRID-001~004`、`TC-PERF-001~003`
- 测试数据复用现有素材：多库隔离用 `test-data/multi-library-1` + `multi-library-2`；千级/滚动用 `test-data/huge-library`（7000+ 文件）或 `test-library`（100 张 × 10 组）

- [ ] **Step 1: 启动应用**

```powershell
conda activate imageviewer
npm run dev
```

- [ ] **Step 2: 逐用例执行并记录**

| 用例 | 操作要点 | 通过标准 |
|------|---------|---------|
| TC-THUMB-004 跨库缓存隔离 | 浏览库 A → 切换库 B | 库 B 无库 A 缩略图 |
| TC-THUMB-005 LRU 淘汰 | huge-library 连续浏览 1000+ 张 | 内存稳定（任务管理器观察进程内存 <500MB） |
| TC-GRID-001~004 虚拟滚动 | huge-library 快速滚动、切换网格/瀑布流 | 无卡顿、DOM 节点数恒定（DevTools 观察） |
| TC-PERF-001 千级加载 | test-library 首次扫描与加载 | 扫描 <30s、首渲染 <3s |
| TC-PERF-002/003 | 按测试方案原文执行 | 滚动 FPS >30 |

同时回归 Phase 1 功能（顺带验证 ledger 标记的两个"缺口"实际已实现）：
- Ctrl 点选、Shift 区间选择（网格 + 瀑布流两种布局）
- 右键菜单全部动作、批量重命名、回收站视图

- [ ] **Step 3: 记录结果文档**

创建 `docs/archive/文件操作阶段测试执行记录.md`（front matter：`type: archive`、`status: archived`），逐用例记录 PASS/FAIL + 实测数据（扫描耗时、内存峰值、FPS 估值）。

- [ ] **Step 4: roadmap 勾选 + Commit**

roadmap 第七节「测试完善」勾选已执行条目。

```powershell
git add docs/
git commit -m "test: 执行归档测试用例并记录结果"
```

---

## Task 5: 扫描性能优化

**Files:**
- Modify: `src/main/services/scanner.ts`

**设计要点：**
- **前置核实（必做）**：先确认 `file_hash` 的消费方。增量跳过用的是「文件大小 + 修改时间」双条件（不比较 hash），`file_hash` 仅为未来重复图查找（#17）预留。若核实发现任何比较新旧 hash 的逻辑，则本任务降级为「仅注释说明，不改窗口」
- hash 窗口 1MB → 64KB：对 10 万张库可减少约 90% 的读取量

- [ ] **Step 1: 核实 hash 消费方**

```powershell
Select-String -Path src\main\*.ts,src\main\**\*.ts -Pattern "file_hash"
```

确认除写入外无「读取旧 hash 与新 hash 比较」的逻辑。

- [ ] **Step 2: 修改 hash 窗口**

`scanner.ts` `calculateFileHash`（约 L400）：

```typescript
const stream = fs.createReadStream(filePath, { start: 0, end: 64 * 1024 });
```

同步更新方法注释（「只计算前 64KB」），并注明：**存量库已有记录的 `file_hash` 口径不同，未来做重复图查找（#17）时需全量重算，不可跨口径比较**。

- [ ] **Step 3: 确认批处理现状**

检查 `scanner.ts` 中 `addImages` 调用是否已按批（每批 ≤100）提交。若已分批则仅在注释确认；若未分批，按 100 条切分后循环调用。

- [ ] **Step 4: 基准测试**

用 `test-data/huge-library` 记录优化前后扫描总耗时（删除该库 `.ivlib` 重新扫描计时，或与 git stash 前的数据对比）。将结果写入 Task 4 的测试记录文档。

- [ ] **Step 5: 全量测试 + Commit**

```powershell
npx vitest run
git add src/main/services/scanner.ts docs/
git commit -m "perf: hash 窗口缩减至 64KB 并记录扫描基准"
```

---

## Task 6: 拆分 selectionStore

**Files:**
- Create: `src/stores/selectionStore.ts`
- Modify: `src/stores/imageStore.ts`
- Modify: `src/components/ImageGrid.tsx`、`src/components/MasonryGrid.tsx`、`src/components/file-ops/BatchRenameDialog.tsx`（如有引用）
- Modify: `src/stores/__tests__/selection.test.ts`（迁移导入）

**设计要点：**
- 拆出范围（`imageStore.ts` L129-136 接口 + L182-183 初始值 + L694-720 实现）：`selectedPaths`、`lastSelectedPath`、`toggleSelection`、`selectRange`、`selectAll`、`clearSelection`、`getSelectedPaths`
- `selectAll` 依赖图片列表：跨 store 读取 `useImageStore.getState().images`（zustand 允许 store 间 getState，避免循环依赖）
- 消费方迁移前先用 grep 找全：`selectedPaths|toggleSelection|selectRange|selectAll|clearSelection|getSelectedPaths|lastSelectedPath`

- [ ] **Step 1: 创建 selectionStore.ts**

```typescript
import { create } from 'zustand'
import { useImageStore } from './imageStore'

interface SelectionState {
  selectedPaths: Set<string>
  lastSelectedPath: string | null
  toggleSelection: (path: string) => void
  selectRange: (fromPath: string, toPath: string, allPaths: string[]) => void
  selectAll: () => void
  clearSelection: () => void
  getSelectedPaths: () => string[]
}

export const useSelectionStore = create<SelectionState>((set, get) => ({
  selectedPaths: new Set<string>(),
  lastSelectedPath: null,

  toggleSelection: (path: string) => {
    const next = new Set(get().selectedPaths)
    if (next.has(path)) next.delete(path); else next.add(path)
    set({ selectedPaths: next, lastSelectedPath: path })
  },

  selectRange: (fromPath, toPath, allPaths) => {
    const fromIdx = allPaths.indexOf(fromPath)
    const toIdx = allPaths.indexOf(toPath)
    if (fromIdx < 0 || toIdx < 0) return
    const rangePaths = allPaths.slice(Math.min(fromIdx, toIdx), Math.max(fromIdx, toIdx) + 1)
    const next = new Set(get().selectedPaths)
    for (const p of rangePaths) next.add(p)
    set({ selectedPaths: next, lastSelectedPath: toPath })
  },

  selectAll: () => {
    const allPaths = useImageStore.getState().images.map(img => img.relative_path)
    set({ selectedPaths: new Set(allPaths) })
  },

  clearSelection: () => set({ selectedPaths: new Set(), lastSelectedPath: null }),

  getSelectedPaths: () => Array.from(get().selectedPaths),
}))
```

- [ ] **Step 2: 迁移消费方**

`ImageGrid.tsx`（L54）与 `MasonryGrid.tsx`（L49）：

```typescript
// 原：const { selectedPaths, lastSelectedPath, toggleSelection, selectRange } = useImageStore()
const { selectedPaths, lastSelectedPath, toggleSelection, selectRange } = useSelectionStore()
```

同文件内 `useImageStore.getState().clearSelection()` 改为 `useSelectionStore.getState().clearSelection()`。`BatchRenameDialog` 及其他引用按同样模式迁移。

- [ ] **Step 3: 从 imageStore 移除已拆出内容**

删除 `ImageState` 接口中多选相关 7 个成员、初始值、实现块（L129-136、L182-183、L694-720）。库切换时清空选择的时机（如有）改为调用 `useSelectionStore.getState().clearSelection()`。

- [ ] **Step 4: 迁移测试** — `selection.test.ts` 改用 `useSelectionStore`；`selectAll` 用例保留 `useImageStore.setState({ images: [...] })` 注入。

- [ ] **Step 5: 验证**

```powershell
npx vitest run
npm run build
```

手动回归：网格/瀑布流 Ctrl 与 Shift 多选、右键批量删除、批量重命名、全选。

- [ ] **Step 6: Commit**

```powershell
git add src/
git commit -m "refactor: 拆出 selectionStore（多选状态独立）"
```

---

## Task 7: 拆分 viewStore

**Files:**
- Create: `src/stores/viewStore.ts`
- Modify: `src/stores/imageStore.ts` + 全部消费组件

**设计要点：**
- 拆出范围：`viewMode`/`setViewMode`、`sidebarOpen`/`toggleSidebar`、`folderSidebarOpen`/`toggleFolderSidebar`、`gridLayoutMode`/`setGridLayoutMode`（含 localStorage 持久化）、`favoriteViewMode`/`setFavoriteViewMode`、`selectedFavoriteFolder`/`setSelectedFavoriteFolder`
- **保留在 imageStore**：排序字段（`imageSortBy`/`imageSortOrder`/`applySort`，因 `loadImages` 直接消费）、`isLoading`/`error`（与数据加载耦合）、扫描进度
- 风险点：`applySort` 内部读取 `favoriteViewMode`（imageStore L675-691），拆出后改为 `useViewStore.getState().favoriteViewMode`
- 消费组件先用 grep 找全：`viewMode|sidebarOpen|folderSidebarOpen|gridLayoutMode|favoriteViewMode|toggleSidebar|toggleFolderSidebar|setViewMode|setGridLayoutMode|setFavoriteViewMode|selectedFavoriteFolder`

- [ ] **Step 1: 创建 viewStore.ts**

```typescript
import { create } from 'zustand'

interface ViewState {
  viewMode: 'grid' | 'list' | 'single'
  sidebarOpen: boolean
  folderSidebarOpen: boolean
  gridLayoutMode: 'grid' | 'masonry'
  favoriteViewMode: 'all' | 'folder' | 'single'
  selectedFavoriteFolder: string | null
  setViewMode: (mode: 'grid' | 'list' | 'single') => void
  toggleSidebar: () => void
  toggleFolderSidebar: () => void
  setGridLayoutMode: (mode: 'grid' | 'masonry') => void
  setFavoriteViewMode: (mode: 'all' | 'folder' | 'single') => void
  setSelectedFavoriteFolder: (folderPath: string | null) => void
}

export const useViewStore = create<ViewState>((set, get) => ({
  viewMode: 'grid',
  sidebarOpen: true,
  folderSidebarOpen: true,
  gridLayoutMode: (localStorage.getItem('gridLayoutMode') as 'grid' | 'masonry') || 'grid',
  favoriteViewMode: 'folder',
  selectedFavoriteFolder: null,

  setViewMode: (mode) => set({ viewMode: mode }),
  toggleSidebar: () => set((state) => ({ sidebarOpen: !state.sidebarOpen })),
  toggleFolderSidebar: () => set((state) => ({ folderSidebarOpen: !state.folderSidebarOpen })),
  setGridLayoutMode: (mode) => {
    set({ gridLayoutMode: mode })
    localStorage.setItem('gridLayoutMode', mode)
  },
  setFavoriteViewMode: (mode) => set({ favoriteViewMode: mode }),
  setSelectedFavoriteFolder: (folderPath) => set({ selectedFavoriteFolder: folderPath }),
}))
```

- [ ] **Step 2: 迁移消费方** — 按组件逐个替换（`App.tsx`、`layout/` 下组件、`MediaFilter`、收藏视图相关组件等），每改一个组件确认编译通过。

- [ ] **Step 3: imageStore 适配** — 移除已拆出成员；`applySort` 中改读 `useViewStore.getState().favoriteViewMode` 与 `selectedFavoriteFolder`。

- [ ] **Step 4: 验证**

```powershell
npx vitest run
npm run build
```

手动回归：F5 切换视图、F6 切换文件夹栏、网格/瀑布流切换（重启后布局记忆仍在）、收藏视图三种模式切换。

- [ ] **Step 5: Commit**

```powershell
git add src/
git commit -m "refactor: 拆出 viewStore（视图/布局/侧边栏状态独立）"
```

---

## Task 8: 最终验证与文档收口

- [ ] **Step 1: 全量验证**

```powershell
conda activate imageviewer
npx vitest run
npm run build
```

确认：全部测试通过（60+ 基线 + 本计划新增）；`imageStore.ts` 主体降至 400 行以下（目标 <300，视 `applySort` 等残留耦合而定）。

- [ ] **Step 2: 完整手动回归清单**

1. 启动 <3s，多库切换正常
2. 网格/瀑布流：Ctrl 单选、Shift 区间、右键菜单全部动作
3. 批量重命名（模式 + 手动）、删除确认对话框、回收站按库过滤
4. 收藏/评分/历史功能无回归
5. 侧边栏、视图切换、排序、布局记忆
6. 性能红线：huge-library 滚动 FPS ≥30、内存 <500MB

- [ ] **Step 3: roadmap 收口**

第七节工程化任务勾选完成项；第九节「imageStore 巨石」行更新为已拆分（`selectionStore` + `viewStore`）；本计划文档移入 `docs/archive/superpowers/plans/` 并标记 archived。

- [ ] **Step 4: 最终 Commit**

```powershell
git add -A
git commit -m "chore: Phase 1.5 技术债清理完成（归档/小修/测试/性能/Store 拆分）"
```

---

## 完成标准

1. 全部 8 个 Task 合并，每个 Task 独立可回滚
2. 测试全部通过（现有 60+ 基线 + 新增多选/边界用例），`npm run build` 无错误
3. 4 个 deferred minor 关闭并有测试或注释佐证
4. 归档的 9 个测试用例执行完毕，结果记录在 `docs/archive/`
5. hash 窗口优化有基准对比数据
6. `imageStore` 拆分为三个 store，巨石状态缓解，全部手动回归项通过
7. 性能红线不突破
8. `docs/roadmap.md` 进度与文档归档状态完全一致

## 与 roadmap.md 的对应关系

| roadmap 条目 | 本计划 Task | 说明 |
|---|---|---|
| 第七节 测试完善 | Task 3, 4 | 单元测试扩充 + 归档用例执行 |
| 第七节 性能优化（Hash 优化/批处理） | Task 5 | 并行扫描已有，文件夹级增量维持暂缓 |
| 第九节 架构隐患（imageStore 巨石） | Task 6, 7 | 拆出 selectionStore + viewStore |
| 第九节 media-registry 令牌持久化 | 不纳入 | 维持「远期需求时再持久化」结论 |
| 第四节 文件夹级增量检测 | 不纳入 | 维持暂缓结论（需 watch 机制） |
