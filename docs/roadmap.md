---
title: 开发路线图
description: 项目功能优化与开发任务总览 — 优先级、进度、执行顺序、技术选型建议
type: roadmap
status: current
updated: 2026-08-16
---

# 开发路线图

> 本文档是项目唯一的任务规划来源，汇总原「功能优化路线图」（08）与「项目后续任务分析」（10），按优先级重新组织。
> 每项标注当前进度。进度为 **2026-08-16 对照代码核实**，非从旧文档照抄。

## 📊 进度图例

| 标记 | 含义 |
|------|------|
| ✅ | 已完成 |
| 🟡 | 部分完成（注明缺口） |
| ⬜ | 未开始 |

---

## 一、高优先级任务 (P0)

### 0. 多媒体支持（视频 + 音频 + 图片统一管理）✅ 已完成

- **实施**：2026-06-22 完成，包括 `media://` 令牌流式加载、wavesurfer.js 音频波形、YARL 图片 lightbox、组件分离（ImageLightbox/AudioViewer/AudioPlayer）、收藏媒体筛选。
- **记录**：方案与验收见 [archive/多媒体模块重构方案.md](archive/多媒体模块重构方案.md)、[archive/多媒体模块重构测试计划.md](archive/多媒体模块重构测试计划.md)。

### 1. 高级搜索面板 ⬜

顶部搜索入口，多维度组合搜索：文件名模糊、尺寸范围、文件大小、格式、拍摄日期范围、收藏状态/评分/标签。

- **预计工时**：1 天
- **涉及文件**：`database.ts`（加搜索方法）、`library-handlers.ts`、`SearchPanel.tsx`（新）、`searchStore.ts`（新）

### 2. 设置壁纸 ⬜

一键将当前图片设为 Windows 桌面壁纸，支持填充/适应/拉伸模式。

- **预计工时**：0.5 天
- **新增依赖**：`npm install wallpaper`
- **涉及文件**：`wallpaper-service.ts`（新）、`ImageViewer.tsx`（工具栏按钮）、右键菜单

### 3. 图片文件操作 ⬜

应用内复制/移动/重命名/删除图片、在资源管理器显示。删除走系统回收站。

- **预计工时**：1 天
- **新增依赖**：`npm install trash`
- **涉及文件**：`file-service.ts`（新）、`library-handlers.ts`、网格/查看器右键菜单

### 4. 增量扫描优化 🟡

进度（2026-08-16 已完成两项）：

- ✅ **「文件大小 + 修改时间」双条件跳过**未变动文件 + **批量预载已有记录**（单次查询 → Map，替代逐文件查询）— `scanner.ts`
- ⬜ **文件夹级增量检测**（只扫描变动的子文件夹）— **暂缓**：目录 mtime 无法反映子目录内文件的就地修改，整树跳过会漏报改动；需 watch/归档位机制才安全，暂不实现以免引入数据不一致

- **涉及文件**：`src/main/services/scanner.ts`

### 5. 浏览历史 ✅ 已完成

进度（2026-08-16 完成）：

- ✅ `history` 表 + `addHistory()`/`getHistory()`/`clearHistory()`（容量上限 500）
- ✅ IPC 暴露 + preload 接口 + `historyStore.ts` + 侧边栏「最近浏览」（缩略图列表、点击跳转原图、清空）
- ✅ 查看器播放/翻页时自动记录（连续重复自动跳过）

> 📌 **收尾建议**：第一轮 P0 小活（浏览历史 + 评分接入 + 增量扫描）已于 **2026-08-16 完成**；下一轮建议从 P0 剩余项（高级搜索）或 P1 的「批量重命名 / 回收站 / 最近添加」开始（见[推荐执行顺序](#九-推荐执行顺序））。

---

## 二、近期任务 (P1)

| # | 功能 | 进度 | 预计工时 | 说明 |
|---|------|------|----------|------|
| 7 | 批量重命名工具 | ⬜ | 1 天 | 命名模板 `{日期}_{序号}`，预览与撤销 |
| 8 | 回收站功能 | ⬜ | 1 天 | 软删除 + 回收站视图恢复/彻底删除 |
| 9 | 最近添加/最近修改视图 | ⬜ | 0.5 天 | 按索引时间 / 文件修改时间排序的虚拟文件夹 |
| 10 | 智能图片分类 | ⬜ | 1 天 | 按拍摄日期/尺寸/格式/纵横比自动分组 |
| 11 | 图片预加载优化 | ⬜ | 1 天 | 查看器相邻图预加载 + 按滚动速度动态调整 |
| 12 | 评分系统 | ✅ | 1 天 | `RatingStars` 接入查看器工具栏；评分隐含收藏；收藏视图按评分排序 |
| 13 | 离线库自动检测 | ⬜ | 0.5 天 | `chokidar` 监听硬盘插拔，自动更新库在线状态 |

---

## 三、中期任务 (P2)

| # | 功能 | 预计工时 |
|---|------|----------|
| 14 | 图片对比模式（并排/滑块，同步缩放平移） | 1.5 天 |
| 15 | EXIF 信息查看器（拍摄时间/相机参数/GPS） | 1 天 |
| 16 | 库统计面板（总数/大小/格式分布/时间线） | 1 天 |
| 17 | 相似/重复图片查找（pHash） | 2 天 |
| 18 | 标签系统（编辑/筛选/标签云） | 2 天 |
| 19 | 缓存管理 UI（占用统计/一键清理/上限设置） | 0.5 天 |
| 20 | 全屏沉浸式模式（F11，自动隐藏 UI） | 0.5 天 |
| 21 | 主题/皮肤系统（深色浅色/强调色/密度） | 1 天 |
| 22 | 幻灯片增强（过渡动画/随机播放/自定义列表） | 1 天 |
| 23 | 文件夹封面设置 | 1 天 |
| 24 | 图片直方图（RGB/亮度/可选显示） | 1 天 |

---

## 四、远期任务 (P3)

| # | 功能 | 预计工时 |
|---|------|----------|
| 25 | 批量格式转换（JPG ↔ PNG ↔ WEBP） | 2 天 |
| 26 | 隐藏/加密文件夹 | 2 天 |
| 27 | 图片联系表/拼图生成 | 2 天 |
| 28 | 时间线视图 | 待评估 |
| 29 | 地图模式（EXIF GPS） | 待评估 |
| 30 | 键盘快捷键自定义 | 待评估 |
| 31 | 多显示器支持（副屏幻灯片） | 待评估 |

---

## 五、Phase 3 — AI / 爬虫（需确认）

| # | 功能 | 预计工时 |
|---|------|----------|
| 32 | AI 标签自动生成（CLIP 模型） | 3-5 天 |
| 33 | 人脸识别分组 | 5-7 天 |
| 34 | 智能筛选最佳照片（构图/清晰度/曝光评分） | 3-5 天 |
| 35 | 超分辨率/去水印等 AI 修图 | 待评估 |
| 36 | 图片爬虫模块 | 待评估 |
| 37 | 云同步（收藏/标签） | 待确认 |
| 38 | 插件系统 | 待确认 |

---

## 六、特色功能建议

### 39. 智能收藏推荐

根据浏览行为（停留时间长、放大查看、反复回看）自动标记"可能喜欢"的图片，提供一键收藏入口。纯行为分析，无需 AI 模型。

- **预计工时**：1-2 天

### 40. 自定义相册

用户可创建多个相册（类似播放列表），跨文件夹、跨库把相关图片归入逻辑分组，支持自定义排序、拖拽调整顺序、封面设置。与收藏的区别：收藏是"我喜欢"（扁平聚合），相册是"同主题归档"（可排序可命名）。

- **预计工时**：2-3 天
- **数据库设计**：

```sql
CREATE TABLE albums (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    cover_image_id INTEGER,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE album_items (
    album_id INTEGER NOT NULL,
    library_id INTEGER NOT NULL,
    image_id INTEGER NOT NULL,
    sort_order INTEGER NOT NULL,
    added_at TEXT DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (album_id, library_id, image_id),
    FOREIGN KEY (album_id) REFERENCES albums(id) ON DELETE CASCADE
);
```

---

## 七、工程化任务

### 测试完善

- [ ] 归档测试方案中的待测试用例（见 [archive/测试方案.md](archive/测试方案.md)）：`TC-THUMB-004/005`、`TC-GRID-001~004`、`TC-PERF-001~003`
- [ ] `scripts/` 目录为空，需按测试方案创建测试数据生成脚本（`generate-test-images.ts`）
- [ ] 补充自动化单元测试（当前 50 个用例已由 `4cdadda` 建立，可持续扩充）

### 性能优化

| 优化点 | 现状 | 预期提升 |
|--------|------|----------|
| 并行扫描（每次 10-20 张批量处理） | 现有扫描 ~4.5ms/张 | 30-50% |
| Hash 优化（1MB 缩减到 64KB） | 现有 | 10-15% |
| 数据库批处理事务（每次 100 条） | 现有 | 20-30% |
| 路径索引优化 | 现有 | 文件夹切换 +50% |

---

## 八、技术选型建议（第三方库）

| 功能 | 推荐库 | 说明 |
|------|--------|------|
| EXIF 解析 | `exifreader` | 读取图片 EXIF 信息 |
| 感知 Hash | `imghash` | 计算图片 pHash |
| 壁纸设置 | `wallpaper` | 跨平台设置壁纸 |
| 地图显示 | `leaflet` | 显示 GPS 位置 |
| 图表统计 | `recharts` | React 图表库 |
| 文件删除（回收站） | `trash` | 删除走系统回收站 |
| 文件监听 | `chokidar` | 离线库检测 |

> 性能基准约束：任何功能不应显著影响启动（<3s）、内存 <500MB、滚动 FPS ≥30。

---

## 九、推荐执行顺序

**第一轮 (1-2 周)**：~~多媒体模块重构~~（已完成）→ ~~浏览历史~~ → ~~评分接入~~ → ~~增量扫描~~（2026-08-16 全部完成）

**第二轮 (2-3 周)**：图片文件操作 → 批量重命名 → 回收站 → 设置壁纸

**第三轮 (3-4 周)**：高级搜索 → 相似图片查找 → 最近添加/修改 → 智能分类

**第四轮 (4-6 周)**：标签系统 → 库统计 → EXIF 查看器 → 测试完善

---

**文档创建日期**：2026-03-11（源）｜**合并重写**：2026-08-16